# _data/navigation.yml
navigation:
  - name: About
    link: "{{ '/about-me/' | relative_url }}"
  - name: Skills
    link: "{{ '/skills/' | relative_url }}"
  - name: Projects
    link: "{{ '/projects/' | relative_url }}"
  - name: Contact
    link: "{{ '/contact/' | relative_url }}"